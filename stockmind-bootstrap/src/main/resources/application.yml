spring:
  config:
    import: optional:classpath:application-agent-runtime.yml
  application:
    name: stockmind-agent
  datasource:
    url:
    username:
    password:
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info

stockmind:
  knowledge:
    vector:
      table: stock_doc_vector
      dimensions: 1024
      default-top-k: 5
