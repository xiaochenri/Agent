spring:
  config:
    import: optional:classpath:application-agent-runtime.yml
  application:
    name: stockmind-agent
  datasource:
    url: 
    username: 
    password: 
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info

javascope:
  user:
    # 开发环境固定用户；接入 Spring Security 后由 CurrentUserProvider 替换。
    development-user-id: ${JAVASCOPE_DEVELOPMENT_USER_ID:chat-user}

stockmind:
  knowledge:
    vector:
      table: stock_doc_vector
      dimensions: 1024
      default-top-k: 5
